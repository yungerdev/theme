import { nord } from "../colors/nord";
import type { ThemeComponent } from "../types";

export const Tooltip: ThemeComponent = {
	styles: {
		root: {
			backgroundColor: nord["PolarNight4"],
		},
	},
};
